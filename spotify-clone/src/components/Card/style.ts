import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    marginHorizontal: 8,
    alignItems: "center",
  },

  image: {
    width: 130,
    height: 130,
    borderRadius: 3,
    marginBottom: 8,
  },

  artistCover: {
    borderRadius: 75,
  },
});
