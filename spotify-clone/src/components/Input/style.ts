import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: "100%",
    maxWidth: 330,
    height: 49,
    borderRadius: 5,
    marginTop: 8,
    marginBottom: 8,
    borderColor: "#FFF",
    borderWidth: 0.7,
    color: "#fff",
    paddingLeft: 20,
  },

  label: {
    width: "100%",
    maxWidth: 330,
    color: "#fff",
    fontWeight: 700,
  },

  container: {
    width: "100%",
    alignItems: "center",
  },
});
