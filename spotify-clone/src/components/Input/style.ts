import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: "100%",
    maxWidth: 330,
    height: 49,
    borderRadius: 5,
    marginTop: 6,
    marginBottom: 6,
    borderColor: "#FFF",
    borderWidth: 0.7,
    color: "#fff",
    paddingLeft: 20,
  },

  label: {
    width: "100%",
    maxWidth: 330,
    color: "#fff",
  },

  container: {
    width: "100%",
    alignItems: "center",
  },
});
