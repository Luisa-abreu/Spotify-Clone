import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: 900,
    color: "#FFF",
    textAlign: "left",
    width: "100%",
    marginBottom: 20,
    marginLeft: 16,
  },
  shelf: {
    paddingBottom: 30,
  },
});
